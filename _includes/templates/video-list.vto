<ul class="video-list">
  {{ for video of search.pages("category=video index!=true", "order=desc") }}
    <li>
      <figure class="video">
        <div class="fluid-width-video-wrapper">
          <lite-youtube
            videoid="{{ video.id }}"
            style="background-image: url('https://i.ytimg.com/vi/{{ video.id }}/hqdefault.jpg')"
          >
            <a
              href="https://youtube.com/watch?v={{ video.id }}"
              class="lyt-playbtn"
              title="Play Video"
            >
              <span class="lyt-visually-hidden">Play Video: {{
                  video.title
                }}</span>
            </a>
          </lite-youtube>
        </div>
        <figcaption class="video-description">
          <a href="{{ video.url }}" class="video-title">
            <h2 class="display">{{ video.title }}</h2>
          </a>
          {{ if video.tags?.length }}
            <p class="post-tags">
              <span class="html-only">🏷️ </span>{{ for tag of video.tags }}
                <a
                  data-pagefind-filter="tag"
                  class="badge"
                  href="/archive/{{ tag }}"
                >{{ tag }}</a>
              {{ /for }}
            </p>
          {{ /if }}
          {{ video.content |> md }}
        </figcaption>
      </figure>
    </li>
  {{ /for }}
</ul>
